/**
 * Swap exchange feature flags.
 * Set a value to false to disable that provider at runtime.
 * @type {{ changelly: boolean, quickex: boolean }}
 */
export const swapExchangeList = Object.freeze({
  changelly: true,
  quickex: true
});
